from util import Graph


def aco(graph: Graph, m: int, alpha: float = 1, beta: float = 5, rho: float = 0.5, q: int = 100, cycles_count: int = 3000):
    if m is None:
        m = len(graph.nodes)

    pass
